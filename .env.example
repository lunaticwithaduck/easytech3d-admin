# Backend admin API (easytech3d-backend).
BACKEND_API_URL=http://localhost:4000
# Shared secret sent to the backend as x-admin-token — must match the backend's ADMIN_TOKEN.
ADMIN_TOKEN=dev-admin-token
# Password the operator types on the login screen.
ADMIN_PASSWORD=admin
